services:
  pi-container:
    command: pi
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ${MODELS_JSON}:/root/.pi/agent/models.json:ro
      - ${PWD}:/app
    working_dir: /app
    tty: false
